#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase, followed by a new line.
 * Return: 0
 */

int main(void)
{
int n;
n = 0;
while (n < 10)
{
printf("%d", n);
n++;
}
printf("\n");
return (0);
}
