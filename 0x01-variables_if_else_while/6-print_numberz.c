#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase, followed by a new line.
 * Return: 0
 */


int main(void)
{
int n;
n = 48;
while (n < 58)
{
putchar(n);
n++;
}
putchar('\n');
return (0);
}
